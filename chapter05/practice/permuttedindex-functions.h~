#ifndef __permuttedindex_functions__
#define __permuttedindex_functions__
#include <string>
#include <vector>
// helper function to find the longest string within vector of words
// returns the length of longest string
std::string::size_type longest_string(const vector<string>& string_vector);

#endif
